 // quota management
const  changeQuota = (user, quota) => {
    return new Promise((resolve, reject) => {
        // const query = 'UPDATE users SET quota = ? WHERE id = ?';
        // connection.query(query, [quota, user.id], (error, results, fields) => {
        //     if (error) reject(error);
        //     resolve(results);
        // });
    })
}